<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron requires JIT for V8. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Disable library validation so Electron's helper bundles load even if signed differently. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- App Sandbox is OFF (we are NOT setting com.apple.security.app-sandbox). -->
  <!-- File access entitlements: respected by macOS even on ad-hoc signed apps. -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
  <key>com.apple.security.files.downloads.read-write</key>
  <true/>
  <!-- Apple Events for `open -a` dispatch and shell.showItemInFolder. -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
  <!-- Inherit access to user-selected and TCC-granted folders in helpers. -->
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
